const fs = require('fs');
const path = require('path');
const filePath = `${__dirname}/../file_form_cli`

// for(let i =0 ; i <= 5; i++){
//     fs.writeFileSync( path.join(filePath, `data-${i}.js`) , "");
// };

// fs.readdir(filePath, (err, fileArr)=>{
//     if(!err) console.log(`====>> fileArr\n`,fileArr);
// })